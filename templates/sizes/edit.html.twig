{% extends 'base.html.twig' %}

{% block title %}Edit Sizes{% endblock %}

{% block body %}
    <h1>Edit Sizes</h1>

    {{ include('sizes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('sizes_index') }}">back to list</a>

    {{ include('sizes/_delete_form.html.twig') }}
{% endblock %}
