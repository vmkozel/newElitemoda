{% extends 'base.html.twig' %}

{% block title %}Collections{% endblock %}

{% block body %}
    <h1>Collections</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ collection.title }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ collection.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collections_index') }}">back to list</a>

    <a href="{{ path('collections_edit', {'id': collection.id}) }}">edit</a>

    {{ include('collections/_delete_form.html.twig') }}
{% endblock %}
