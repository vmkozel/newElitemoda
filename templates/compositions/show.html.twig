{% extends 'base.html.twig' %}

{% block title %}Compositions{% endblock %}

{% block body %}
    <h1>Compositions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ composition.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ composition.title }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ composition.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compositions_index') }}">back to list</a>

    <a href="{{ path('compositions_edit', {'id': composition.id}) }}">edit</a>

    {{ include('compositions/_delete_form.html.twig') }}
{% endblock %}
